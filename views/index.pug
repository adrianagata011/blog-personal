doctype html
html
  head
    title= title
  body
    h1= title
    ul
      if posts && posts.length
        each post in posts
          li
            h2= post.title
            p= post.content
      else
        li No hay publicaciones disponibles.
